markdown
# CSV Data Transformer

Консольная утилита для трансформации и агрегации данных из CSV-файлов с визуализацией в виде таблиц.

##  Установка
Установите необходимые библиотеки:
```bash
pip install tabulate pytest


 Запуск
Для генерации отчета используйте команду:
bash
python main.py --file data1.csv data2.csv --report average_gdp


 Тестирование
Запуск всех тестов (включая проверку через subprocess):
bash
pytest tests/


🏗 Масштабируемость
Проект поддерживает добавление новых типов отчетов через наследование:
В файле app/services/csv_file_reading.py создайте класс, наследовав его от ProcessingMethod.
В методе __init__ переопределите заголовки: self.headers = ['Name1', 'Name2']. 
И не забудье super().__init__(*args, **kwargs) прописать.
В методе method_for_data опишите логику обработки словаря self.country_dict.
Добавьте ваш класс в словарь REPORTS в файле main.py.
Пример:
python
class InflationReport(ProcessingMethod):
    def __init__(self, *args, **kwargs):
        super().__init__(*args, **kwargs)
        self.headers = ['Country', 'Avg Inflation']

    def method_for_data(self):
        # Логика расчета
        return [('Germany', 2.5)]


 Структура проекта
main.py — точка входа и регистрация отчетов.
app/models/ — модели данных (dataclasses). (для сбора и трансформации данных. Метод __post_init__)
app/services/ — чтение CSV и алгоритмы обработки (класс, который обрабатывает файлы и содержит методы для отчета ).
app/running_scripts - логика обработки argparse
tests/ — интеграционные тесты и фикстуры.

### Пример работы
#### Отчет по ВВП
![first](https://github.com/user-attachments/assets/9a1991a7-9e3b-45af-9bb9-8cd02a6c9d89)

#### Детализация данных
![second](https://github.com/user-attachments/assets/48360a02-512a-4315-8311-a59db0d2e57b)


